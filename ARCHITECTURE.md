# Architecture

Copilot Capability Manager 系統架構設計。

---

## 🚨 核心約束：Agent 外層作業

### 系統邊界

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                         實際系統約束                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌───────────────────────────┐              ┌───────────────────────┐  │
│   │      Our Software         │              │      Copilot          │  │
│   │    (Capability Manager)   │  ══════════► │    (黑盒 Agent)       │  │
│   │                           │   唯一通道    │                       │  │
│   │  ✅ 我們可控制            │  AGENTS.md   │  ❌ 我們無法控制       │  │
│   │  ✅ 結構化數據            │  .prompt.md  │  ❌ 內部狀態未知       │  │
│   │  ✅ 設計時驗證            │  (純文字!)   │  ❌ 執行過程不透明     │  │
│   └───────────────────────────┘              └───────────────────────┘  │
│                                                                          │
│              具象化 ─────────────────────────► 抽象化                    │
│          (Graph, Contract)               (Markdown Prompt)              │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 我們的定位

| 特性 | LangGraph/AutoGen | 本專案 |
|------|------------------|--------|
| 位置 | Agent 內部 | **Agent 外部** |
| 控制 | 編排 Agent 執行 | **編譯 Prompt 給 Agent** |
| 通道 | API 調用 | **純文字檔案** |
| 執行 | 直接執行 | **委託 Agent 執行** |

### 核心洞察

> **我們不是在「執行」能力，而是在「編譯」能力成 Prompt**

```text
┌─────────────────────────────────────────────────────────────────────────┐
│                   Capability = Prompt 的元表示                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                          │
│   ┌─────────────────┐      ┌─────────────────┐      ┌───────────────┐   │
│   │   Capability    │      │     Prompt      │      │   Markdown    │   │
│   │     Graph       │ ───► │    Compiler     │ ───► │    Output     │   │
│   │   (元表示)      │      │    (編譯器)     │      │  (給 Agent)   │   │
│   └─────────────────┘      └─────────────────┘      └───────────────┘   │
│                                                                          │
│   類比：TypeScript ──► tsc ──► JavaScript                                │
│         Capability ──► compiler ──► Prompt Markdown                      │
│                                                                          │
└─────────────────────────────────────────────────────────────────────────┘
```

### 「具象化」的真正目的

| 目的 | 說明 | 最終輸出 |
|------|------|----------|
| **設計時驗證** | 檢查流程邏輯、契約一致性 | 無（內部用） |
| **視覺化編輯** | GUI 拖拉、連線 | 無（內部用） |
| **版本管理** | 追蹤能力演進 | 無（內部用） |
| **Prompt 編譯** | 把結構轉成文字 | ✅ `.prompt.md` |

---

## 🧠 Neuro-Symbolic AI 架構

本專案實現了 **Neuro-Symbolic AI** 架構模式，結合符號推理的可解釋性與神經網路的靈活性：

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Neuro-Symbolic Architecture                           │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                   🔷 SYMBOLIC LAYER (符號層)                         │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│   │   │ Capability  │  │   Node      │  │   Branch    │                 │   │
│   │   │   Graph     │  │ Contracts   │  │   Logic     │                 │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│   │   • 結構化的能力定義                                                  │   │
│   │   • 可驗證的契約系統                                                  │   │
│   │   • 確定性的分支邏輯                                                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                     │                                        │
│                                     ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                   🔶 SKILL BRIDGE (技能橋接層)                        │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│   │   │   LLM       │  │  Abstract   │  │   Skill     │                 │   │
│   │   │  Reasoning  │  │  Node       │  │  Executor   │                 │   │
│   │   │             │  │  Resolver   │  │             │                 │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│   │   • Agent 透過 Skills 操作符號結構                                    │   │
│   │   • LLM 決策 + 符號約束 = 可靠執行                                    │   │
│   │   • 抽象節點的動態綁定                                                │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                     │                                        │
│                                     ▼                                        │
│   ┌─────────────────────────────────────────────────────────────────────┐   │
│   │                   🔻 TOOL LAYER (工具層)                             │   │
│   │   ┌─────────────┐  ┌─────────────┐  ┌─────────────┐                 │   │
│   │   │    MCP      │  │   File      │  │  External   │                 │   │
│   │   │   Tools     │  │   System    │  │    APIs     │                 │   │
│   │   └─────────────┘  └─────────────┘  └─────────────┘                 │   │
│   │   • 結構化 API 調用                                                   │   │
│   │   • 確定性的操作執行                                                  │   │
│   │   • 可追蹤的執行結果                                                  │   │
│   └─────────────────────────────────────────────────────────────────────┘   │
│                                                                              │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 為什麼是 Neuro-Symbolic？

| 特性 | 純 Neural (LLM) | 純 Symbolic | Neuro-Symbolic (本專案) |
|------|----------------|-------------|------------------------|
| 靈活性 | ✅ 高 | ❌ 低 | ✅ 高 |
| 可解釋性 | ❌ 黑箱 | ✅ 透明 | ✅ 透明 |
| 可靠性 | ⚠️ 不穩定 | ✅ 穩定 | ✅ 穩定 |
| 適應性 | ✅ 強 | ❌ 弱 | ✅ 強 |

### 三層協作模式

```
User Request: "幫我寫一篇 AI 醫療的報告"
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│ 🔷 Symbolic: Capability Graph 定義執行流程                    │
│    start → search → read → write → validate → end            │
│    (確定性的結構，可驗證)                                       │
└──────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│ 🔶 Skill Bridge: LLM Agent 執行決策                           │
│    - 抽象節點 "read" → 偵測 PDF → 選擇 pdf-reader             │
│    - 分支判斷 → 根據文獻數量選擇路徑                            │
│    (神經網路的靈活決策 + 符號的約束)                            │
└──────────────────────────────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│ 🔻 Tool Layer: MCP Tools 實際執行                             │
│    - mcp_pubmed_search_search_literature(query="AI 醫療")    │
│    - read_file(path="article.pdf")                          │
│    - create_file(path="report.md", content="...")           │
│    (結構化 API，確定性執行)                                    │
└──────────────────────────────────────────────────────────────┘
```

---

## DDD 架構實現

基於 Neuro-Symbolic 理念，我們採用 **領域驅動設計 (DDD)** 實現 Python 核心引擎：

```
src/capability_engine/
├── domain/                      # 🔷 領域層 (Symbolic Core)
│   ├── value_objects/           # 不可變值物件
│   │   ├── node_type.py         # NodeType, EdgeType, ExecutionStatus
│   │   ├── complexity.py        # ComplexityMetrics, ComplexityLevel
│   │   └── contract.py          # NodeContract, Implementation, BranchCondition
│   └── entities/                # 實體（聚合根）
│       ├── node.py              # GraphNode
│       ├── edge.py              # GraphEdge
│       └── graph.py             # CapabilityGraph (Aggregate Root)
│
├── application/                 # 🔶 應用層 (Skill Bridge)
│   ├── use_cases/               # 用例
│   │   └── execute_capability.py # ExecuteCapabilityUseCase
│   └── services/                # 應用服務
│       └── resolver.py          # NodeResolverService, GraphValidatorService
│
├── infrastructure/              # 🔻 基礎設施層 (Tool Integration)
│   ├── mcp/                     # MCP Server
│   │   └── server.py            # CapabilityMCPServer (6 tools)
│   └── prompt/                  # Prompt 生成
│       └── generator.py         # PromptGenerator, PromptInjector
│
└── presentation/                # 呈現層 (TypeScript Extension)
    └── (VS Code Extension)
```

---

```
┌─────────────────────────────────────────────────────────────────────┐
│                         VS Code Editor                               │
├─────────────────────────────────────────────────────────────────────┤
│                                                                      │
│  ┌──────────────────────────────────────────────────────────────┐   │
│  │              Copilot Capability Manager Extension             │   │
│  ├──────────────────────────────────────────────────────────────┤   │
│  │                                                               │   │
│  │  ┌─────────────────┐    ┌─────────────────────────────────┐  │   │
│  │  │  Skill Manager  │    │     Capability Builder          │  │   │
│  │  │    (Webview)    │    │        (Webview)                │  │   │
│  │  │                 │    │                                  │  │   │
│  │  │  ┌───────────┐  │    │  ┌─────┐ ┌─────┐ ┌─────┐       │  │   │
│  │  │  │ Skill List│  │    │  │ S1  │→│ S2  │→│ S3  │       │  │   │
│  │  │  ├───────────┤  │    │  └─────┘ └─────┘ └─────┘       │  │   │
│  │  │  │ + Add New │  │    │                                  │  │   │
│  │  │  │ ⚙️ Edit   │  │    │  [Generate Prompt]              │  │   │
│  │  │  │ 🔧 MCP    │  │    │                                  │  │   │
│  │  │  └───────────┘  │    └─────────────────────────────────┘  │   │
│  │  └─────────────────┘                                          │   │
│  │                                                               │   │
│  ├───────────────────────────────────────────────────────────────┤   │
│  │                        Core Services                          │   │
│  │  ┌──────────────┐ ┌──────────────┐ ┌──────────────────────┐  │   │
│  │  │SkillService  │ │CapService   │ │  McpService          │  │   │
│  │  └──────────────┘ └──────────────┘ └──────────────────────┘  │   │
│  └───────────────────────────────────────────────────────────────┘   │
│                                                                      │
├──────────────────────────────────────────────────────────────────────┤
│                        GitHub Copilot Chat                           │
│  ┌────────────────────────────────────────────────────────────────┐ │
│  │  /cp.xxx → Load .github/prompts/cp.xxx.prompt.md → Execute     │ │
│  └────────────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                          File System                                  │
│  ┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐   │
│  │ .claude/skills/  │  │ .github/prompts/ │  │   memory-bank/   │   │
│  │                  │  │                  │  │                  │   │
│  │  skill-a/        │  │  cp.xxx.prompt   │  │  progress.md     │   │
│  │   └─ SKILL.md    │  │  cp.yyy.prompt   │  │  context.md      │   │
│  │  skill-b/        │  │                  │  │  decisions.md    │   │
│  │   └─ SKILL.md    │  │                  │  │                  │   │
│  └──────────────────┘  └──────────────────┘  └──────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
                                   │
                                   ▼
┌──────────────────────────────────────────────────────────────────────┐
│                         MCP Servers                                   │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐               │
│  │ pubmed-mcp   │  │ zotero-mcp   │  │  other-mcp   │               │
│  └──────────────┘  └──────────────┘  └──────────────┘               │
└──────────────────────────────────────────────────────────────────────┘
```

---

## 組件設計

### 1. VS Code Extension

**入口點**：`src/extension.ts`

```typescript
// 擴充套件啟動
export function activate(context: vscode.ExtensionContext) {
    // 註冊 Skill Manager View
    const skillManagerProvider = new SkillManagerProvider(context);
    context.subscriptions.push(
        vscode.window.registerWebviewViewProvider('skillManager', skillManagerProvider)
    );
    
    // 註冊 Capability Builder View
    const capabilityBuilderProvider = new CapabilityBuilderProvider(context);
    context.subscriptions.push(
        vscode.window.registerWebviewViewProvider('capabilityBuilder', capabilityBuilderProvider)
    );
    
    // 註冊 Commands
    registerCommands(context);
}
```

### 2. Skill Manager (Webview)

**功能**：

| 功能 | 說明 |
|------|------|
| 列表瀏覽 | 顯示所有 Skills，可搜尋過濾 |
| 新增 Skill | 表單輸入：名稱、描述、類別、觸發詞 |
| 編輯 Skill | 修改 Skill 設定和 Prompt |
| MCP 整合 | 選擇 MCP Tools，設定參數 |
| 刪除 Skill | 確認後刪除 |

**資料結構**：

```typescript
interface Skill {
    id: string;
    name: string;
    description: string;
    category: 'research' | 'documentation' | 'git' | 'maintenance' | 'architecture';
    triggers: string[];
    mcpTools?: McpToolConfig[];
    prompt: string;
}

interface McpToolConfig {
    server: string;
    tool: string;
    parameters: Record<string, any>;
}
```

### 3. Capability Builder (Webview)

**功能**：

| 功能 | 說明 |
|------|------|
| 拖拉排序 | 從 Skill 列表拖到工作流程 |
| 步驟設定 | 每個步驟的參數和條件 |
| 流程預覽 | 視覺化工作流程 |
| 生成 Prompt | 匯出 `.prompt.md` 檔案 |

**資料結構**：

```typescript
interface Capability {
    id: string;
    name: string;
    description: string;
    steps: CapabilityStep[];
}

interface CapabilityStep {
    order: number;
    skillId: string;
    parameters?: Record<string, any>;
    condition?: string;
}
```

### 4. Core Services

#### SkillService

```typescript
class SkillService {
    // CRUD 操作
    async listSkills(): Promise<Skill[]>;
    async getSkill(id: string): Promise<Skill>;
    async createSkill(skill: Skill): Promise<void>;
    async updateSkill(id: string, skill: Skill): Promise<void>;
    async deleteSkill(id: string): Promise<void>;
    
    // 檔案操作
    async loadFromFile(path: string): Promise<Skill>;
    async saveToFile(skill: Skill): Promise<void>;
}
```

#### CapabilityService

```typescript
class CapabilityService {
    // CRUD 操作
    async listCapabilities(): Promise<Capability[]>;
    async createCapability(cap: Capability): Promise<void>;
    
    // 生成 Prompt
    async generatePromptFile(cap: Capability): Promise<string>;
}
```

#### McpService

```typescript
class McpService {
    // 偵測 MCP Servers
    async discoverServers(): Promise<McpServer[]>;
    
    // 取得 Tool Schema
    async getToolSchema(server: string, tool: string): Promise<ToolSchema>;
    
    // 列出可用 Tools
    async listTools(server: string): Promise<McpTool[]>;
}
```

---

## 資料流

### Skill 建立流程

```
User → Skill Manager GUI
         │
         ├─ 填寫基本資訊 (name, description, category)
         │
         ├─ 選擇 MCP Tools (optional)
         │   └─ McpService.listTools() → 顯示可用 Tools
         │   └─ 設定 Tool 參數
         │
         ├─ 編寫 Prompt
         │
         └─ 儲存
              │
              └─ SkillService.createSkill()
                   │
                   └─ 寫入 .claude/skills/{id}/SKILL.md
```

### Capability 建立流程

```
User → Capability Builder GUI
         │
         ├─ 從 Skill 列表拖動到工作流程
         │
         ├─ 排列順序 (S1 → S2 → S3)
         │
         ├─ 設定每個步驟參數
         │
         └─ 生成 Prompt
              │
              └─ CapabilityService.generatePromptFile()
                   │
                   └─ 寫入 .github/prompts/cp.{id}.prompt.md
```

### 執行流程

```
User → Copilot Chat: /cp.xxx
         │
         └─ VS Code 載入 .github/prompts/cp.xxx.prompt.md
              │
              └─ Agent Mode 執行
                   │
                   ├─ Step 1: 參考 .claude/skills/s1/SKILL.md
                   │   └─ 呼叫 MCP Tools (如設定)
                   │
                   ├─ Step 2: 參考 .claude/skills/s2/SKILL.md
                   │
                   └─ Step N: memory-updater → 更新 Memory Bank
```

---

## 檔案格式

### SKILL.md 格式

```markdown
---
name: my-skill
description: 技能說明
category: research
triggers:
  - trigger1
  - trigger2
mcpTools:
  - server: pubmed-search
    tool: search_literature
    parameters:
      limit: 10
---

# 技能名稱

## 描述
詳細說明...

## 執行步驟
1. 步驟一
2. 步驟二

## MCP Tools 使用
- `search_literature`: 搜尋文獻
```

### Prompt File 格式 (自動生成)

```markdown
---
description: "📝 工作流程說明"
---

# 工作流程名稱

## Step 1: Skill A
📖 技能參考: `.claude/skills/skill-a/SKILL.md`
...

## Step 2: Skill B
📖 技能參考: `.claude/skills/skill-b/SKILL.md`
...
```

---

## UI 設計

### Skill Manager

```
┌─────────────────────────────────────┐
│  🧩 Skill Manager         [+ New]   │
├─────────────────────────────────────┤
│  🔍 [Search skills...]              │
├─────────────────────────────────────┤
│  📁 Research                        │
│    ├─ 🔎 web-search                 │
│    └─ 📚 literature-review          │
│  📁 Documentation                   │
│    ├─ 📝 report-generator           │
│    └─ 📄 readme-updater             │
│  📁 Git                             │
│    ├─ 📦 git-precommit              │
│    └─ 🚀 git-pusher                 │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  📝 web-search              [Edit]  │
├─────────────────────────────────────┤
│  Description: 網路/文獻檢索          │
│  Category: Research                 │
│  Triggers: search, 搜尋, 檢索        │
│                                     │
│  🔧 MCP Tools:                      │
│    • pubmed-search.search_literature│
│    • pubmed-search.fetch_article    │
│                                     │
│  [View Prompt] [Delete]             │
└─────────────────────────────────────┘
```

### Capability Builder

```
┌─────────────────────────────────────────────────────────┐
│  🔗 Capability Builder                   [Save] [Export]│
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Available Skills          │  Workflow: write_report   │
│  ┌───────────────────┐     │  ┌─────────────────────┐  │
│  │ 🔎 web-search     │ ──→ │  │ 1. 🔎 web-search   │  │
│  │ 📝 report-gen     │     │  │    ↓               │  │
│  │ 📄 report-format  │ ──→ │  │ 2. 📝 report-gen   │  │
│  │ 💾 memory-update  │     │  │    ↓               │  │
│  │ ...               │ ──→ │  │ 3. 📄 report-format│  │
│  └───────────────────┘     │  │    ↓               │  │
│                            │  │ 4. 💾 memory-update│  │
│                            │  └─────────────────────┘  │
│                            │                           │
│                            │  [Preview] [Generate]     │
└─────────────────────────────────────────────────────────┘
```

---

## 開發計劃

詳見 [ROADMAP.md](ROADMAP.md)
