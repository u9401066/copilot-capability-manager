# Prompt Files 機制說明

本文件說明 Copilot Capability Manager 的運作機制。

## 概述

本專案利用 **VS Code Prompt Files** 功能實現自定義斜線指令 `/cp.xxx`，讓使用者可以快速觸發預定義的 Capability（能力流程）。

## 核心概念

### 概念層級

```
Tool (工具)     → 單一 API 調用（如 MCP Tool）
Skill (技能)    → 多 Tools 編排，完成一個明確任務
Capability (能力) → 動態 Skill 狀態機，可迴圈、分支、跨對話
```

### 1. Prompt Files

VS Code 1.99+ 支援在 `.github/prompts/` 目錄放置 `.prompt.md` 檔案，這些檔案會被識別為可在 Copilot Chat 中使用的斜線指令。

**位置**：`.github/prompts/`

**格式**：
```markdown
---
description: "指令說明"
---

# 指令內容
...
```

**重要**：`.github/prompts/` 必須位於 **workspace root**，VS Code 不會遞迴搜尋子目錄。

### 2. Skills（技能）

Skills 是可重用的原子能力，每個 skill 專注於單一任務。

**位置**：`.claude/skills/{skill-id}/SKILL.md`

**範例**：
- `web-search` - 網路檢索
- `report-generator` - 報告產出
- `memory-updater` - Memory Bank 同步

### 3. Capabilities（能力）

Capability 是多個 Skills 的動態組合，定義於 Prompt File 中。與 Workflow 不同，Capability 支援迴圈和條件分支。

**執行方式**：Agent 根據任務狀況動態決定使用哪些 Skills

## 運作流程

```
┌─────────────────────────────────────────────────────────┐
│  用戶在 Copilot Chat 輸入 /cp.write_report              │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  VS Code 載入 .github/prompts/cp.write_report.prompt.md │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Prompt 內容注入到 Agent Mode（保留完整工具權限）          │
└────────────────────────┬────────────────────────────────┘
                         ↓
┌─────────────────────────────────────────────────────────┐
│  Agent 根據 Prompt 動態執行 Skills：                      │
│    - web-search（可能多次迴圈）                           │
│    - report-generator                                    │
│    - report-formatter                                    │
│    - memory-updater                                      │
└─────────────────────────────────────────────────────────┘
```

## 關鍵設計決策

### 為什麼不使用 `agent:` 欄位？

在 Prompt File 中使用 `agent: xxx` 會切換到自定義 agent，導致：
- 失去標準 Agent Mode 的完整工具權限
- 無法使用 MCP 工具（如 PubMed 搜尋）
- 無法執行終端機命令

**我們的做法**：直接在 Prompt File 中寫完整步驟，不指定 agent，這樣會保持在標準 Agent Mode。

### 為什麼 Prompt File 要在 workspace root？

VS Code 的 Prompt Files 功能**只搜尋 workspace root** 的 `.github/prompts/` 目錄。

如果你的專案結構是：
```
c:\workspace\              ← VS Code 開啟這個
└── my-project\
    └── .github\prompts\   ← 不會被識別！
```

解決方案：
1. 直接開啟 `my-project` 作為 workspace
2. 或在 workspace root 建立 `.github/prompts/`

## 擴展能力

### 新增 Skill

1. 建立目錄：
   ```
   .claude/skills/my-skill/
   ```

2. 建立 SKILL.md：
   ```markdown
   ---
   name: my-skill
   description: 我的技能說明
   ---
   
   # 我的技能
   
   ## 描述
   ...
   
   ## 執行流程
   1. ...
   2. ...
   ```

### 新增 Capability

1. 建立 Prompt File：
   ```
   .github/prompts/cp.my-capability.prompt.md
   ```

2. 定義步驟（Agent 會動態執行）：
   ```markdown
   ---
   description: "我的能力流程"
   ---
   
   # 我的 Capability
   
   ## Step 1: xxx
   📖 技能參考: `.claude/skills/xxx/SKILL.md`
   
   ## Step 2: yyy
   📖 技能參考: `.claude/skills/yyy/SKILL.md`
   ```

3. 更新 `cp.help.prompt.md`

## 與 SpecKit 的差異

| 項目 | SpecKit | 本專案 |
|------|---------|--------|
| 使用 agent | ✅ 切換到自定義 agent | ❌ 保持 Agent Mode |
| 工具權限 | 受限於 agent 定義 | 完整權限 |
| 工作流程 | 單一 prompt | 多步驟 Capability |
| 迴圈支援 | 無 | ✅ Agent 動態決定 |

## 檔案結構

```
copilot-capability-manager/
├── .github/
│   ├── prompts/                    # Prompt Files
│   │   ├── cp.write_report.prompt.md
│   │   ├── cp.commit.prompt.md
│   │   ├── cp.new_skill.prompt.md
│   │   └── ...
│   └── bylaws/                     # 子法規
├── .claude/
│   └── skills/                     # Skill 模組
│       ├── web-search/
│       │   └── SKILL.md
│       ├── report-generator/
│       │   └── SKILL.md
│       └── ...
├── memory-bank/                    # 專案記憶
├── AGENTS.md                       # Agent 指引
└── README.md                       # 專案說明
```

## 相關資源

- [VS Code Prompt Files 文檔](https://code.visualstudio.com/docs/copilot/copilot-customization#_prompt-files)
- [AGENTS.md](../AGENTS.md) - Agent 指引
- [CONSTITUTION.md](../CONSTITUTION.md) - 專案憲法
