# Skill Pathway 設計文檔

> 🛤️ **Capability = Validated Skill Pathway**
> 
> 不只是 workflow，而是經過驗證的「能力路徑」

## 核心概念：Skill Pathway vs Workflow

| 特性 | 一般 Workflow | Skill Pathway (我們的創新) |
|------|--------------|---------------------------|
| 定義 | 步驟序列 | **有向圖** + 使用頻率權重 |
| 驗證 | 無 | **通過條件驗證才算能力** |
| 視覺化 | 線性流程圖 | **網狀拓撲圖 + 熱力圖** |
| 複用 | 複製貼上 | **Skills 節點可多次引用** |
| 統計 | 無 | **使用頻率、成功率、時間** |

---

## 1. Skill Pathway 圖結構

### 1.1 有向加權圖

```
          ┌─────────────────┐
          │   web-search    │ (使用次數: 45)
          │   🔍 Research   │
          └────────┬────────┘
                   │ (35)
                   ▼
          ┌─────────────────┐
          │ report-generator│ (使用次數: 32)
          │   📝 Document   │
          └────────┬────────┘
                   │ (28)
         ┌─────────┼─────────┐
         │         │         │
         ▼         ▼         ▼
    ┌────────┐ ┌────────┐ ┌────────┐
    │ format │ │ review │ │ export │
    │   📄   │ │   👁️   │ │   💾   │
    └────────┘ └────────┘ └────────┘
         \        │        /
          \       │       /
           ▼      ▼      ▼
          ┌─────────────────┐
          │ memory-updater  │ (使用次數: 89)
          │   🧠 Sync       │
          └─────────────────┘
```

### 1.2 資料模型

```typescript
// src/types/skillPathway.ts

interface SkillNode {
  id: string;
  skillId: string;
  position: { x: number; y: number };
  
  // 統計數據
  stats: {
    usageCount: number;      // 使用次數
    successRate: number;     // 成功率 (0-1)
    avgDuration: number;     // 平均執行時間 (ms)
    lastUsed: Date;
  };
}

interface SkillEdge {
  id: string;
  source: string;  // SkillNode id
  target: string;  // SkillNode id
  weight: number;  // 使用頻率（邊的粗細）
  
  // 條件 (optional)
  condition?: {
    type: 'always' | 'on_success' | 'on_failure' | 'custom';
    expression?: string;
  };
}

interface SkillPathway {
  id: string;
  name: string;
  description: string;
  
  // 圖結構
  nodes: SkillNode[];
  edges: SkillEdge[];
  
  // 入口和出口
  entryNodes: string[];  // 起始節點
  exitNodes: string[];   // 結束節點
  
  // 驗證規則
  validation: ValidationRule[];
  
  // 統計
  stats: PathwayStats;
  
  // 狀態
  status: 'draft' | 'testing' | 'validated' | 'certified';
  
  createdAt: Date;
  updatedAt: Date;
}
```

---

## 2. 視覺化設計

### 2.1 Pathway Graph View

使用 D3.js 或 React Flow 實作：

```
┌──────────────────────────────────────────────────────────────┐
│  🛤️ Skill Pathway: write_report                              │
│  Status: ✅ Validated  |  Success Rate: 94.2%                │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│       ○ web-search ─────┬─────→ ○ report-gen                │
│       │  (45 uses)      │         │ (32 uses)                │
│       │                 │         │                          │
│       │    ┌────────────┘         ▼                          │
│       │    │              ○ format ○ review ○ export        │
│       │    │                   │       │        │            │
│       │    │                   └───────┴────────┘            │
│       │    │                           │                     │
│       │    └──────────────────────────→▼                     │
│       │                         ○ memory-updater             │
│       │                           (89 uses)                  │
│       │                                                      │
│       └──[on_failure]─────────→ ○ error-handler             │
│                                                              │
│  ═══════════════════════════════════════════════════════════│
│  📊 Legend:                                                  │
│  ● Node size = usage count                                   │
│  ─ Edge width = transition frequency                         │
│  🟢 Success path  🔴 Failure path  ⚪ Conditional             │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 熱力圖模式 (Heatmap)

顯示使用頻率：

```
┌──────────────────────────────────────────────────────────────┐
│  🔥 Usage Heatmap (Last 30 days)                             │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   memory-updater    ████████████████████████████  89 uses   │
│   web-search        ██████████████████            45 uses   │
│   report-generator  █████████████                 32 uses   │
│   git-commit        ████████████                  28 uses   │
│   format-output     ████████                      18 uses   │
│   code-review       ██████                        12 uses   │
│   error-handler     ███                            6 uses   │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 2.3 時間軸視圖 (Timeline)

```
┌──────────────────────────────────────────────────────────────┐
│  📅 Execution Timeline                                       │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  12:00 ├─ web-search ──────────┤ 2.3s                       │
│  12:02 ├─ report-gen ─────────────────────┤ 5.1s            │
│  12:07 ├─ format ────┤ 0.8s                                 │
│  12:08 ├─ memory-updater ─┤ 0.5s                            │
│        ▲                                                     │
│        └── Total: 8.7s (avg: 9.2s)                          │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 3. 能力驗證機制 (Capability Validation)

### 3.1 驗證等級

| 等級 | 名稱 | 條件 | 徽章 |
|------|------|------|------|
| 0 | Draft | 剛建立 | ⚪ |
| 1 | Testing | 執行 > 5 次 | 🔵 |
| 2 | Validated | 成功率 > 80%, 執行 > 20 次 | 🟢 |
| 3 | Certified | 通過人工審核 + 測試案例 | ⭐ |
| 4 | Production | 正式環境使用 > 100 次 | 🏆 |

### 3.2 驗證規則

```typescript
interface ValidationRule {
  id: string;
  name: string;
  type: 'auto' | 'manual';
  
  // 自動驗證條件
  autoCondition?: {
    minSuccessRate: number;      // 最低成功率
    minExecutions: number;       // 最低執行次數
    maxAvgDuration: number;      // 最大平均時間
    requiredOutputs: string[];   // 必須產出
  };
  
  // 人工驗證
  manualChecklist?: {
    item: string;
    required: boolean;
  }[];
}
```

### 3.3 驗證流程

```
┌─────────────┐     ┌─────────────┐     ┌─────────────┐
│   Draft     │ ──→ │   Testing   │ ──→ │  Validated  │
│  建立 Pathway │     │  累積數據    │     │  自動通過    │
└─────────────┘     └─────────────┘     └─────────────┘
                                               │
                                               ▼
                    ┌─────────────┐     ┌─────────────┐
                    │ Production  │ ←── │  Certified  │
                    │  正式使用    │     │  人工審核    │
                    └─────────────┘     └─────────────┘
```

### 3.4 驗證報告

```markdown
## Capability Validation Report

**Pathway**: write_report
**Status**: ✅ Validated
**Validation Date**: 2025-12-20

### Metrics
| Metric | Value | Threshold | Status |
|--------|-------|-----------|--------|
| Executions | 45 | ≥ 20 | ✅ |
| Success Rate | 94.2% | ≥ 80% | ✅ |
| Avg Duration | 8.7s | ≤ 30s | ✅ |
| Required Outputs | 3/3 | 100% | ✅ |

### Test Cases
- [x] Case 1: Basic report generation
- [x] Case 2: Multi-source research
- [x] Case 3: Error handling

### Certification Notes
- Approved by: @user
- Comments: "Ready for production use"
```

---

## 4. 統計儀表板

### 4.1 Overall Dashboard

```
┌──────────────────────────────────────────────────────────────┐
│  📊 Skill Pathway Analytics                                  │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Total Pathways: 8        Total Skills: 22                  │
│  ✅ Validated: 5          ⭐ Certified: 2                    │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ Weekly Usage Trend                                      │ │
│  │                                                  ●      │ │
│  │                                           ●    ╱       │ │
│  │                              ●      ●    ╱    ╱        │ │
│  │         ●      ●      ●    ╱      ╱    ╱             │ │
│  │    ●   ╱      ╱      ╱    ╱      ╱                    │ │
│  │   ╱   ╱      ╱      ╱                                 │ │
│  │──Mon──Tue──Wed──Thu──Fri──Sat──Sun──                   │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  Top Skills This Week:                                       │
│  1. 🧠 memory-updater   +12%                                │
│  2. 🔍 web-search       +8%                                 │
│  3. 📝 report-gen       +5%                                 │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 Pathway Detail

```
┌──────────────────────────────────────────────────────────────┐
│  🛤️ Pathway: write_report                    ⭐ Certified   │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│  Success Rate      Duration          Usage                  │
│  ┌──────────┐      ┌──────────┐      ┌──────────┐          │
│  │   94.2%  │      │   8.7s   │      │   45     │          │
│  │ ████████ │      │ ████▓▓▓▓│      │ ████████ │          │
│  │ ▲ +2.1%  │      │ ▼ -1.2s │      │ ▲ +12    │          │
│  └──────────┘      └──────────┘      └──────────┘          │
│                                                              │
│  Skill Flow Distribution:                                    │
│  ┌────────────────────────────────────────────────────────┐ │
│  │ web-search → report-gen: 78%                           │ │
│  │ report-gen → format: 45%                               │ │
│  │ report-gen → review: 33%                               │ │
│  │ report-gen → export: 22%                               │ │
│  │ * → memory-updater: 100%                               │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

---

## 5. 創新差異化

### 5.1 vs 一般 Workflow 工具

| 功能 | Zapier/n8n | GitHub Actions | **Skill Pathway** |
|------|------------|----------------|-------------------|
| 圖結構 | 線性 | DAG | **有向圖 + 環路** |
| 驗證 | 無 | 手動測試 | **自動驗證 + 認證** |
| 統計 | 基礎 | 基礎 | **深度分析 + 熱力圖** |
| AI 整合 | 部分 | 無 | **原生 MCP 支援** |
| 複用性 | 模板 | 可組合 | **節點級複用** |

### 5.2 核心創新點

1. **Pathway = Validated Workflow**
   - 不只是「可以跑」，而是「證明有效」
   
2. **有向圖 + 權重**
   - 支援分支、合併、循環
   - 邊的權重反映真實使用模式

3. **能力認證系統**
   - 從 Draft → Testing → Validated → Certified
   - 類似軟體的 CI/CD + Code Review

4. **深度統計**
   - 使用熱力圖
   - 成功率趨勢
   - 執行時間分析

---

## 6. 實作優先順序

### Phase 1: 基礎視覺化
- [ ] Skill 節點顯示（使用 React Flow）
- [ ] 邊連接顯示
- [ ] 基礎統計（使用次數）

### Phase 2: 統計收集
- [ ] 執行追蹤 Hook
- [ ] 成功/失敗記錄
- [ ] 時間測量

### Phase 3: 驗證機制
- [ ] 自動驗證規則
- [ ] 驗證狀態顯示
- [ ] 驗證報告生成

### Phase 4: 進階視覺化
- [ ] 熱力圖模式
- [ ] 時間軸視圖
- [ ] 儀表板

---

## 7. 相關技術

- **圖視覺化**: [React Flow](https://reactflow.dev/), [D3.js](https://d3js.org/)
- **統計圖表**: [Recharts](https://recharts.org/), [Chart.js](https://www.chartjs.org/)
- **狀態管理**: [Zustand](https://zustand-demo.pmnd.rs/), [Jotai](https://jotai.org/)
